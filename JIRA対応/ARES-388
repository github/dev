https://jira.teamx.tokyo/browse/ARES-388


serverless-common
https://bitbucket.teamx.tokyo/projects/ARES/repos/ares/browse/packages/serverless-common/src/lib/config.ts#57,83
这两个地方的值改为false

本地跑服务:
1. yarn   -- 下载第三方包
2. yarn bootstrap  -- 编译共通包
3. yarn serverless:authenticate:dev     启动本地认证服务
4. yarn serverless:management:dev       启动本地管理服务
5. yarn serverless:restaurant:dev       启动你们测试的服务
        serverless:ticket:dev
        serverless:questionnaire:dev
        serverless:content:dev
        serverless:live:dev
        。。。。
        
6. yarn web:all-submodule:build       编译所有web包
7. ~/development/bitbucket.teamx.tokyo/ares/packages/web-management/src/environments/environment.development.ts
   确认地址 保证本地一样
8. yarn web:management:dev

服务启动后访问:
http://wytest.test.com:4200/welcome

修改shop-setting.component.html

yarn web:restaurant:build 或者 yarn web:restaurant:dev(修改保存后自动编译)




改完代码后:
1.点+,改的部分就跑到Staged Changes里去了
2.git commit 或者 yarn commit
3.git push
4.去JIRA ARES-388页面查看最新commit
5.创建拉请
6.継続する
7.作成する


productCodeWithSmaregiNotice: 'スマレジ連携の場合、1~9で始まる数字1桁から15桁までの商品コードを設定してください。',
productCodeWithSmaregiNotice: '联动Smaregi的情况下，商品号必须是以1~9的数字开头的15位以内的数字。',
productCodeWithSmaregiNotice: 'When binding with Smaregi, prodcut code must be numbers within 15 digits that starts with 1~9.',

有冲突

1.git pull origin 先把development的代码pull下来，和本地的merge.
2.有冲突的地方，决定一个，accept
3.yarn commit
4.git push

需要再改:

取完代码发现没有分支可以切换

执行 git fetch origin

选分支，改代码，staging,yarn commit,git push

这里其实是：
git fetch [alias]
以上命令告诉 Git 去获取它有而你没有的数据


